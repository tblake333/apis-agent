# Development environment configuration (HOST-BASED)
#
# Use this when running probe FROM THE HOST via TCP connection:
#   cp .env.dev .env && python main.py --env
#
# Note: This requires x86 fdb libraries on the host. For most cases,
# use ./run-probe.sh instead (runs inside container).

# Database Configuration (TCP connection to probe container)
PROBE_DB_PATH=localhost:3050/Microsip datos/microsip.fdb
PROBE_DB_USER=sysdba
PROBE_DB_PASSWORD=masterkey
PROBE_DB_CHARSET=UTF8

# Worker Configuration
PROBE_MAX_WORKERS=4
PROBE_INTAKE_POSITION=1

# Cloud Sync Configuration (pointing to mock API on localhost)
PROBE_CLOUD_ENDPOINT=http://localhost:8080/api/changes
PROBE_CLOUD_API_KEY=dev-test-key
PROBE_BUFFER_PATH=probe_buffer_dev.db
PROBE_ENABLE_BACKGROUND_RETRY=true
PROBE_CLOUD_ENABLED=true
