# Firebird database driver
fdb>=2.0.0

# HTTP requests for cloud sync
requests>=2.28.0

# Environment variable loading from .env files
python-dotenv>=1.0.0

# GUI dependencies (cross-platform tray application)
pystray>=0.19.0
Pillow>=10.0.0

# macOS tray support (pystray backend)
# Install on macOS only: pip install pyobjc-framework-Cocoa
# pyobjc-framework-Cocoa>=9.0; sys_platform == "darwin"
